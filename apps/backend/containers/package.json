{
  "name": "yomi-server-containers",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy --config ../wrangler.toml",
    "deploy:storage": "wrangler deploy --config ../wrangler.storage.toml",
    "deploy:storage:prod": "wrangler deploy --config ../wrangler.storage-prod.toml",
    "test": "node --experimental-strip-types --test *.test.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cloudflare/containers": "~0.3.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
