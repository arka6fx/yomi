# Exclude the landing workspace the backend doesn't need — keeps the image small
# and avoids pulling the Next toolchain onto the server.
apps/landing

# Never copy installed deps, build output, or local env/secrets into the image.
**/node_modules
**/dist
**/.turbo
**/*.log
.git
.env
.env.*
!.env.production.example
